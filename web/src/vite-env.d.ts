/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/react" />
/// <reference types="vite-plugin-pwa/client" />

// The git commit this bundle was built from, injected at build time by Vite's
// `define` (see vite.config.ts / vitest.config.ts). Empty in unstamped dev
// builds.
declare const __APP_COMMIT__: string;
